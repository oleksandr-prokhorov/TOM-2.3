--[[
	
	Module: OnPlayerDeath.lua
	Autor: Bimbol
	
	OnPlayerDeath
	
]]--

function OnServerWorldTime(oldHour, oldMinute, newHour, newMinute)
	--UpdateServerTime(oldHour, oldMinute, newHour, newMinute)
	
	-- BE_Callback
	BE_OnServerWorldTime(oldHour, oldMinute, newHour, newMinute)
end

function BE_OnServerWorldTime(oldHour, oldMinute, newHour, newMinute)
end

-- Loaded
